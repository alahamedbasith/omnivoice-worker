# RunPod Serverless Worker — Python dependencies
# NOTE: torch and torchaudio are already installed in the base image via conda.
#       Do NOT re-install them here — causes pip/conda conflicts.

# ── RunPod SDK ────────────────────────────────────────────────────────────────
runpod>=1.6.0

# ── torchvision (not in base image, must install separately) ──────────────────
torchvision==0.20.1         # matches torch 2.5.1 in base image

# ── Transformers — latest version needed for HiggsAudioV2TokenizerModel ───────
transformers>=4.52.0

# ── Audio I/O ─────────────────────────────────────────────────────────────────
soundfile>=0.12.1
numpy>=1.24.0

# ── HuggingFace model download ────────────────────────────────────────────────
huggingface_hub>=0.20.0

# ── OmniVoice ─────────────────────────────────────────────────────────────────
git+https://github.com/k2-fsa/OmniVoice.git